{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run JavaFX (Maven)",
      "type": "shell",
      "command": "mvn",
      "args": ["-q", "-DskipTests", "javafx:run"],
      "group": { "kind": "build", "isDefault": true }
    }
  ]
}
